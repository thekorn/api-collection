profiles:
  bunteProduction:
    name: production - bunte
    data:
      endpoint: https://metaview-api.delight.video
      licenseKey: 5412c3162c8e822561eae05be9eead48393d22ec
      secret: "{{env.BUNTE_XYMATIC_SECRET}}"
  bunteStaging:
    name: staging - bunte
    data:
      endpoint: https://api.burdaforward-staging.greensuite.io
      licenseKey: 99aeb12e937547588ce95e995e6ae3d586eb6e85
      secret: "{{env.BUNTE_XYMATIC_SECRET}}"

.ignore:
  base: &base
    headers:
      Accept: application/json

requests:
  metadata: !folder
    name: Video - Metadata
    requests:
      getVideoMetadata: !request
        <<: *base
        name: GET metadata of video
        method: GET
        url: "{{endpoint}}/v1/get_video/{{licenseKey}}/97b69ce45222ddb89d9768d0999cbded1a88943e"
        authentication: !bearer "{{secret}}"
